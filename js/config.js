/**
 * Configuração de produção.
 *
 * Para ativar o banco compartilhado, substitua os dois valores abaixo pelos
 * dados públicos do seu projeto Supabase. Sem eles, o sistema usa IndexedDB e
 * funciona localmente no navegador.
 */
export const APP_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: "",
};

